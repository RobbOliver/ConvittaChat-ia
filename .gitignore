# dependencies
node_modules/

# build output
dist/
build/

# env — never commit real secrets (OPENROUTER_API_KEY lives here)
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# testing / coverage
coverage/

# IDE / OS
.DS_Store
.vscode/
.idea/

# prompt preview/debug output saved locally
/tmp-output/

# local scratch copies of flow JSONs built during flow-design sessions — not source of truth
# (the flow itself lives in the app's database, this is just a working reference)
/flows/
